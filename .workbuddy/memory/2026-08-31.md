# 2026-08-31

## Inno Setup 安装包 (ISS) 创建
- 创建 `plans\mcp-servers-for-revit-安装脚本.iss`：参照 SEPD CAD 项目 ISS 风格，支持 Revit R20-R26 多版本切换
  - `skipserver` 模式：不包含 runtime/（无 node_modules），仅打包插件 DLL + .addin
  - 安装目标：`%APPDATA%\Autodesk\Revit\Addins\{year}`
  - 无需管理员权限（`PrivilegesRequired=lowest`）
  - 含中文/英文双语言、卸载清理、安装前冲突检测
  - 配套 `build_installer_iss.ps1`：先 `build.ps1 -SkipServer`，再自动调用 ISCC 编译

## 自解压 EXE 安装包（IExpress 替代方案）
- IExpress 在 headless 环境无法运行（纯 GUI 应用），改用 .NET 自解压 stub + ZIP appended 方案
- 创建 `dist/stub/Program.cs`：C# 自解压 stub，通过解析 ZIP EOCD 记录准确定位 appended ZIP
- 编译为 self-contained trimmed 独立 EXE (13 MB)
- 拼接 ZIP (67 MB) 生成最终 installer.exe (80 MB)
- ZIP 内部文件：install.ps1 + .addin + revit_mcp_plugin/（含 runtime/）
- 执行流程：stub.exe → 提取 embedded ZIP → 运行 install.ps1 → 复制到 Revit Addins 目录
- 注意：当前 build 输出已包含 runtime/（先前非 skipserver 构建产生）

## ISS/安装体系重构
- 发现三个关键问题：
  - `build.ps1` 从未编译 `plugin\RevitMCPPlugin.csproj`，导致 build 输出缺少 `RevitMCPPlugin.dll`
  - 顶层 `RevitMCPCommandSet\` 目录是多余的——代码从 `revit_mcp_plugin\Commands\RevitMCPCommandSet\{year}\` 加载 DLL
  - ISS 有三个分离的 Source 入口（.addin, RevitMCPCommandSet, revit_mcp_plugin）互相不协调
- 重构 `build.ps1`：新增 plugin 编译步骤 + 全部 staging 到 `build\` 单目录
  - build 输出路径从硬编码的 `plugin\bin\AddIn...\revit_mcp_plugin\runtime` 改为 staging 到 `build\revit_mcp_plugin\runtime`
  - command.json 从项目根复制到 `build\revit_mcp_plugin\Commands\RevitMCPCommandSet\`
- 重构 ISS：单入口 `Source: "..\build\*"`，去掉顶层 RevitMCPCommandSet
- 简化 `build_installer_iss.ps1`：直接调 build.ps1 + ISCC，不再检测 AddIn 目录

## MCP 连接失败排查与恢复（8-31 上午）
- 现象：MCP 工具全部 "Method not found"，插件日志 84 条「创建命令实例失败 [xxx]: RevitMCPCommandSet.dll」
- 根因：`Nice3point.Revit.Api.RevitAPI` 的 `Version="$(RevitVersion).*"` 通配符在 R26 上自动选了 26.4.10（本机 Revit 2026 实为 26.0.4.409），导致 CommandSet/Plugin 编译后引用 26.4.x → 运行时 "Assembly version conflict"
- 修复：`commandset/RevitMCPCommandSet.csproj` 与 `plugin/RevitMCPPlugin.csproj` 对 `$(RevitVersion)=='2026'` 锁 `2026.0.*`，其余版本保留通配符；新增 `NoWarn;NU1605;NU1608` 抑制降级错误
- 编译注意：配置名含空格，须 `dotnet build -c "Debug R26"`；DLL 被 Revit 锁定时先 `taskkill /F /IM Revit.exe` 再 cp
- 结果：重启 Revit 后命令实例化仍一度全失败，怀疑 `RevitMCPSDK.dll`（26.0.0.73）引用匹配；用户随后"全部更新"后，实测 `say_hello`/`get_current_view_info`/`get_current_view_elements` 三个命令均正常返回真实数据 → MCP 连接恢复
- 待办：验证 `create_swept_shape` XY 路径轮廓旋转修复（代码已改，重启后未实测）

## 功能数量核对（87 工具 vs 测试报告 81 条）
- 权威口径：MCP 服务器实际暴露 **87 个工具** = 84 个 Revit 命令（对应 `commandRegistry.json`，100% 覆盖）+ 3 个本地数据库工具（`store_project_data`/`query_stored_data`/`store_room_data`，不走 Revit）
- 工具名 ≠ 命令名共 3 处：`color_elements`→`color_splash`、`tag_all_walls`→`tag_walls`、`tag_all_rooms`→`tag_rooms`
- 测试报告问题：仅覆盖 80/87（`place_schedule_on_sheet` 重复计 1 次），漏测 `get_current_view_info`/`set_parameters`/`manage_view_filters`/`manage_graphics_resources` + 3 个数据库工具；分类标题「查询/分析(8)」实为 10、「MEP(7)」实为 8

## Inno Setup 安装包 RestartManager 崩溃修复
- 现象：Setup.exe 运行时崩溃，日志 `Calling RestartManager's RmGetList` → `Format '可以使用 RestartManager 避免重新启动? %s ' invalid or incompatible with argument`
- 根因：汉化版 `Default.isl`（风铃夜思雨编译）中该消息用 C 风格 `%s` 占位，Inno Setup 6.2.0 运行时 Format() 参数不匹配
- **关键区分（此前误判，务必记住）**：
  - `CloseApplications=no` → 完全不调用 RestartManager API（不执行 RmGetList），**治本开关**
  - `RestartApplications=no` → 仅控制"安装完是否重启被关闭的程序"，**是有效指令**，但不影响 RmGetList 是否执行
- 修复（`plans\mcp-servers-for-revit-安装脚本.iss`）：
  1. `[Setup]` 加 `CloseApplications=no` + `RestartApplications=no`
  2. 注释掉 `[Languages] chinesesimp`（避免加载损坏中文消息表）
  3. `[Code] InitializeSetup` 里 `Exec('taskkill','/F /IM node.exe /T',...)` 释放 runtime\node.exe 文件锁
- **Pascal 语法坑**：`[Code]` 段注释必须用 `//` 或 `{ }`；用 `;` 会导致 ISCC 编译失败（`;` 是语句结束符，不是注释符）
- ISCC 路径（非标准，不在 Program Files）：`C:\_PROM_\DEV\Inno Setup 6\ISCC.exe`
- 验证：重新编译成功（43.4s），产出 `dist\mcp-servers-for-revit_R26_2026_Setup.exe`（38.2MB），无警告
- 副作用提示：`taskkill node.exe` 会结束本机【所有】Node.js 进程

## 修复 Revit MCP commandRegistry.json 为空（12:06-12:27）
- `Commands/commandRegistry.json` 中 `commands` 数组为空 `[]`，导致插件 TCP 服务端收到 JSON-RPC 后全部返回 `"Method not found"`
- 从 `Commands/RevitMCPCommandSet/command.json` 将 67 个命令定义（含 `say_hello`、`get_current_view_info` 等）填充到 `commandRegistry.json`
- **原因**：`commandRegistry.json` 是插件运行时读取的命令注册表，`command.json` 是命令集源定义，安装/部署后没做同步
- **修复方式**：直接写入 67 个命令到 `commandRegistry.json`
- **注意**：需要重启 Revit 使插件重新加载注册表后生效
- **下次修改 MCP 工具时须同时更新两个 JSON**：`commandRegistry.json`（运行时）和 `RevitMCPCommandSet/command.json`（源定义）
